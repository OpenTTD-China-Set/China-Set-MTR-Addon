// Begin FIRST_EMUCOACH

// Graphics


// Vehicle




switch (FEAT_TRAINS, SELF, switch_first_emucoach_capacity, position_in_articulated_veh % 3) {
    1: return 0;
    return 0;
}

switch (FEAT_TRAINS, SELF, switch_first_emucoach_additional_text, param_cargo_decay) {
    return string(STR_DESC_2, string(STR_ATTACH_MTR_FIRST_EMU), string(STR_COMFORT_240));
}